services:
  database:
    build:
      context: ./web/database
  backend:
    build:
      context: ./web/backend
    volumes:
      - ./web/backend:/app
      - backend_node_modules:/app/node_modules
    ports:
      - "3000:3000"
    depends_on:
      - database
volumes:
  backend_node_modules:
